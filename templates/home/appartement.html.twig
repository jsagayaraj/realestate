{% extends "base.html.twig" %}
{% block title 'Annonce Louer' %}

    {% block body %}
        {# include menu #}
        {% include "include/menu.html.twig" %}


        <header class="secondary" id="head">
            <div class="container">
                <div class="row">
                    <h1>Appartement</h1>
                </div>
            </div>
        </header>

        <!-- container -->
        <section class="container">
            <div
                class="row">
                <!-- main content -->
                {% for type in types %}
                    <div class="col-lg-4 col-md-4 col-sm-12">
                        <div class="newsBox">
                            <div class="thumbnail">
                                <figure>
                                    <img alt="" src="{{vich_uploader_asset(type, 'imageFile')}}"></figure>
                                <div class="caption maxheight2">
                                    <div class="box_inner">
                                        <div class="box">
                                            <p class="title">
                                                <strong>{{type.title}}</strong>
                                            </p>
                                            <p>Lorem ipsum dolor sit amet, conc tetu er adipi scing. Praesent ves tibuum molestie lacuiirhs. Aenean.</p>
                                        </div>
                                        <div>
                                            <a class="btn-inline" href="#">more</a>
                                        </div>
                                    </div>
                                </div>
                            </div>
                        </div>
                    </div>
                {% endfor %}
                <!-- /main -->
            </div>
        </section>


        {# include footer #}
        {% include "include/footer.html.twig" %}

    {% endblock %}
