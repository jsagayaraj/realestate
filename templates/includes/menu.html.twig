<!--/ Nav Star /-->
<nav class="navbar navbar-default navbar-trans navbar-expand-lg fixed-top">
  <div class="container">
    <button aria-controls="navbarDefault" aria-expanded="false" aria-label="Toggle navigation" class="navbar-toggler collapsed" data-target="#navbarDefault" data-toggle="collapse" type="button">
      <span></span>
      <span></span>
      <span></span>
    </button>
    <a class="navbar-brand text-brand" href="{{path('home')}}">Estate<span class="color-b">Agency</span>

    </a>
    <button aria-expanded="false" class="btn btn-link nav-search navbar-toggle-box-collapse d-md-none" data-target="#navbarTogglerDemo01" data-toggle="collapse" type="button">
      <span aria-hidden="true" class="fa fa-search"></span>
    </button>
    <div class="navbar-collapse collapse justify-content-center" id="navbarDefault">
      <ul class="navbar-nav">
        <li class="nav-item">
          <a class="nav-link" href="{{path('annonce_louer')}}">Louer</a>
        </li>
        <li class="nav-item">
          <a class="nav-link" href="{{path('annonce_vendre')}}">Vender</a>
        </li>
        <li class="nav-item">
          <a class="nav-link" href="{{path('annonces')}}">Tous les Biens</a>
        </li>
        <li class="nav-item">
          <a class="nav-link" href="{{path('contact')}}">Contact</a>
        </li>
        

 {% if is_granted('ROLE_ADMIN') %}
        <li class="nav-item">
          <a class="nav-link" href="{{path('admin')}}">Dashboard</a>
        </li>
{% endif %} 

      </ul>
    </div>
    <div class="dropdown">
      <button aria-expanded="false" aria-haspopup="true" class="dropmenu" data-toggle="dropdown">
      {% if not app.user %}
        <i class="fa fa-user fa-2x"></i>
      
       {% elseif app.user %}
        Bonjour {{app.user.gender}} {{app.user.lastname}}
        {% endif %}
        
     </button>
      <div aria-labelledby="dropdownMenuButton" class="dropdown-menu">
      {% if not app.user %}
      
        <a class="dropdown-item" href="{{path('login')}}">Me connecter</a>
        <a class="dropdown-item" href="{{path('registration')}}">Créer un compte</a>
      {% endif %}
        {% if app.user %}
        <a class="dropdown-item" href="{{path('profil')}}">Profile</a>
        <a class="dropdown-item" href="{{path('logout')}}">Déconnection</a>
      {% endif %}

      </div>
    </div>

  </div>
</nav>
<!--/ Nav End /-->
